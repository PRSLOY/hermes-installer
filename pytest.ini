[pytest]
# Pin the rootdir to the repo root regardless of the invocation directory, so the
# root conftest.py (which makes `backend.*` and sibling-test imports work) is always
# loaded. Without an inifile here, `pytest` started from inside tests/ chose
# tests/ as rootdir and the root conftest was skipped (issue #17).
testpaths = tests
